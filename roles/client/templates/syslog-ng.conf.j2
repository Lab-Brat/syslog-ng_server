@version: 3.35
@include "scl.conf"

source s_local {
    system(); internal();
};

destination d_syslog_tcp {
    syslog("{{ log_server }}" transport("tcp") port(601));
};

log {
    source(s_local);destination(d_syslog_tcp);
};
